<div class="container-fluid">
  <p id="notice"><%= notice %></p>

  <h1><%= @company.name %></h1>
  <p>Email: <%= @company.email %></p>

  <div class="btn-group">
    <%= link_to 'Edit', edit_company_path(@company), :class => "btn btn-small" %>
    <%= link_to 'Back', companies_path, :class => "btn btn-small" %>
  </div>
</div>
