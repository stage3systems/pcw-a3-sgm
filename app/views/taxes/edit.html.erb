<div class="container-fluid">
  <h1>Editing tax</h1>

  <%= render 'form' %>

  <div class="btn-group">
    <%= link_to 'Show', @tax, :class => 'btn' %>
    <%= link_to 'Back', taxes_path, :class => 'btn' %>
  </div>
</div>
