<% content_for :navbar_buttons do %>
  <%= link_to 'Cancel',
              @terminal ? port_terminal_tariffs_path(@port, @terminal) : port_tariffs_path(@port),
              class: 'btn btn-default btn-sm' %>
<% end %>
<div class="row">
  <div class="col-md-12">
    <h2 class="title">New tariff for <%= @port.name %><%= " / #{@terminal.name}" if @terminal %></h2>

    <%= render 'form' %>
  </div>
</div>
