<div class="container-fluid">
  <h1>Editing charge for <%= @port.name %></h1>

  <%= render 'form' %>

  <div class="btn-group">
    <%= link_to 'Show', @port, :class => 'btn' %>
    <%= link_to 'Back', port_charges_path(@port), :class => 'btn' %>
  </div>
</div>
