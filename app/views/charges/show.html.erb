<div class="container-fluid">
  <p id="notice"><%= notice %></p>

  <h1>Charge for <%= @port.name %></h1>

  <div class="btn-group">
    <%= link_to 'Edit', edit_port_charge_path(@port, @charge), :class => "btn" %>
    <%= link_to 'Back', port_charges_path(@port), :class => "btn" %>
  </div>
</div>
