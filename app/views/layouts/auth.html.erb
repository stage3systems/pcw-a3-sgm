<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= "#{@title} | " if defined?(@title) %><%= ProformaDA::Application.config.tenant_name %> | PDA Estimator</title>
  <!--[if lt IE 9]>
  <script src="https://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= stylesheet_link_tag ProformaDA::Application.config.tenant, media: "all" %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= favicon_link_tag ProformaDA::Application.config.tenant_favicon, :rel => 'shortcut icon' %>
  <%= csrf_meta_tags %>
  <%= render partial: 'common/piwik' %>
  <style>
    html, body { min-height: 100%; height: 100%; }
    body { background: url(<%= asset_path 'bg_login.jpg' %>) center center no-repeat; background-size: cover; }
    .login-container { width: 300px; margin: 20px auto 0; padding: 15px; border: 1px solid rgba(255,255,255,0.3); font-family: "HelveticaNeue-Light", "Helvetica Neue Light", "Helvetica Neue", Helvetica, Arial, "Lucida Grande", sans-serif; font-weight: 300; }
    .login-container h1 { margin: 40px 0 50px; color: #fff; text-align: center; }
    .login-container form { margin: 0; }
    .login-container input { width: 100%; padding: 10px 12px; background: rgba(255,255,255,0.3); border: none; display: block; border-radius: 3px; }
    .login-container input.username { margin-bottom: 1px; border-radius: 3px 3px 0 0; } /* assuming only 2 input fields */
    .login-container input.password { border-radius: 0 0 3px 3px; }
    .login-container input[type=text]:focus { background: #fff; }
    .login-container input[type=password]:focus { background: #fff; }
    .login-container button.submit { margin-top: 20px; padding: 10px; background: rgba(0,0,0,0.25); color: #fff; width: 100%;}
    .login-container button.submit:hover,
    .login-container button.submit:focus { background: rgba(0,0,0,0.4); }
    .login-container .forgot-pass { margin: 20px 0; text-align: center; }
    .login-container .forgot-pass a { color: rgba(255,255,255,0.5); }

    .login-footer { width: 300px; margin: 0 auto; padding: 20px 0; font-size: 11px; color: rgba(255,255,255,0.3); text-align: center; }
    .login-footer a { margin-left: 20px; color: rgba(255,255,255,0.3); }

    /* error / alerts */
    .login-container .alert { margin: -15px -15px 0 -15px; background: #a94442; border: none; color: #ebccd1; border-radius: 0; }

    /* style placeholder text */
    .login-container input::-webkit-input-placeholder { color: rgba(255,255,255,0.7); }
    .login-container input::-moz-placeholder { color: rgba(255,255,255,0.7); }
    .login-container input:-ms-input-placeholder { color: rgba(255,255,255,0.7); }


    @media (min-width: 768px){
      .login-container { margin-top: 10%; }
    }

    /* browser checks */
    .browsers { color: #fff; border: 1px solid #fff;}
    .browsers ul > li {list-style: none;}
    .browsers ul > li > a {height: 100px; min-width: 200px; padding: 30px 0 0 120px; background-image: url(<%= asset_path 'logo_browsers.png' %>); background-repeat: no-repeat; text-decoration: none; display: block;}
    .browsers ul > li > a span {font-weight: bold; display: block;}
    .browsers li.ie a {background-position: 0 0;}
    .browsers li.ff a {background-position: 0 -100px;}
    .browsers li.safari a {background-position: 0 -200px;}
    .browsers li.chrome a {background-position: 0 -300px;}

  </style>
</head>
<body>
<div class="container-fluid">
  <%= yield %>
</div>
</body>
</html>
